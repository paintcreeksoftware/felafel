{
  "name": "@felafel/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Wire-contract surface shared across the Felafel codebase. Re-exports DB-backed schemas from @felafel/contracts and adds the IPC/Tailscale-specific types (Channels, OrchestratorStatus, TailscaleStatus, DesktopApi) that have no DB counterpart. Workspace-only; never published.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./traced-ipc": "./src/traced-ipc.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "format": "oxfmt",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@felafel/contracts": "workspace:*",
    "@felafel/logs": "workspace:*",
    "@opentelemetry/api": "^1.9.0"
  },
  "peerDependencies": {
    "electron": "*"
  },
  "peerDependenciesMeta": {
    "electron": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^5.9.3"
  }
}
