{
  "name": "@felafel/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Wire contract types shared across the Felafel codebase — Electron IPC channels, orchestrator HTTP API schemas, and worker dispatch/run schemas. Workspace-only; never published.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint --deny-warnings",
    "format": "oxfmt",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
