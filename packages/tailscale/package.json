{
  "name": "@felafel/tailscale",
  "version": "0.0.0",
  "private": true,
  "description": "Scaffold for the @felafel/tailscale workspace package — destination for the Tailscale manager, parsers/classifiers, worker IP helper, and pill UI currently scattered across apps/desktop and apps/worker. Implementation lands per PAI-139. Workspace-only; never published.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint --deny-warnings",
    "format": "oxfmt",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^5.9.3"
  }
}
