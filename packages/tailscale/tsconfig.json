// Hub config for @felafel/tailscale — splits TS compilation in two so the
// node-side TailscaleManager doesn't see DOM/JSX globals and the renderer-
// side Pill doesn't see node globals. Mirrors the same split apps/desktop
// uses for main/renderer.
{
  "files": [],
  "references": [
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.ui.json" },
    { "path": "./tsconfig.tools.json" }
  ]
}
