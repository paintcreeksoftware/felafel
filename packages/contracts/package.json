{
  "name": "@felafel/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Source-of-truth schemas for the Felafel codebase \u2014 Drizzle table definitions plus wire-shape Zod schemas (with enum unions sourced from the Drizzle columns so DB and wire stay in sync). @felafel/shared re-exports for wire consumers; @felafel/db imports for the runtime client + queries. Workspace-only; never published.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./zod": "./src/zod.ts"
  },
  "scripts": {
    "format": "oxfmt",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "1.0.0-rc.2-33e80cd",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.4.1",
    "fast-check": "^4.7.0",
    "pathe": "^2.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
